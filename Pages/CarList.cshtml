@page
@model MyApp.Namespace.CarListModel
@{
}

@foreach (var Mark in Model.Marks)
{
    <table>
    <tr>
        <td>@Mark.Name</td>
    </tr>
    @foreach (var car in Mark.Models)
    {
        
        <tr>
            <tr>
                <td><a asp-area="" asp-page="Car" asp-route-name="@Mark.Name" asp-route-model="@car.Model">@car.Model</a></td>
            </tr>
            <tr>
                <td>Horse Power</td>
                <td>@car.Engine.HorsePower</td>
            </tr>
            <tr>
                <td>Engine Capacity</td>
                <td>@car.Engine.Capacity</td>
            </tr>
        </tr>
       
    }
    </table>
}

